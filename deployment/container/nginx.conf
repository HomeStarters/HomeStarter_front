server {
    listen 8080;
    server_name localhost;

    # ============================================================
    # API Proxy 설정 (주석 처리)
    # - 현재: Backend Ingress (homestarter-api.172.30.1.205.nip.io)를 통해 API 호출
    # - 직접 프록시가 필요한 경우 아래 주석을 해제하고 ConfigMap의 HOST를 ""로 변경
    # ============================================================

    # # API Proxy - User Service
    # location /users/ {
    #     proxy_pass http://user-service.homestarter-ns.svc.cluster.local:80/users/;
    #     proxy_set_header Host $host;
    #     proxy_set_header X-Real-IP $remote_addr;
    #     proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    #     proxy_set_header X-Forwarded-Proto $scheme;
    # }

    # # API Proxy - Asset Service
    # location /api/v1/assets/ {
    #     proxy_pass http://asset-service.homestarter-ns.svc.cluster.local:80/api/v1/assets/;
    #     proxy_set_header Host $host;
    #     proxy_set_header X-Real-IP $remote_addr;
    #     proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    #     proxy_set_header X-Forwarded-Proto $scheme;
    # }

    # # API Proxy - Housing Service
    # location /housings/ {
    #     proxy_pass http://housing-service.homestarter-ns.svc.cluster.local:80/housings/;
    #     proxy_set_header Host $host;
    #     proxy_set_header X-Real-IP $remote_addr;
    #     proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    #     proxy_set_header X-Forwarded-Proto $scheme;
    # }

    # # API Proxy - Loan Service
    # location /api/v1/loans/ {
    #     proxy_pass http://loan-service.homestarter-ns.svc.cluster.local:80/api/v1/loans/;
    #     proxy_set_header Host $host;
    #     proxy_set_header X-Real-IP $remote_addr;
    #     proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    #     proxy_set_header X-Forwarded-Proto $scheme;
    # }

    # # API Proxy - Loan Admin Service
    # location /api/v1/admin/loans/ {
    #     proxy_pass http://loan-service.homestarter-ns.svc.cluster.local:80/api/v1/admin/loans/;
    #     proxy_set_header Host $host;
    #     proxy_set_header X-Real-IP $remote_addr;
    #     proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    #     proxy_set_header X-Forwarded-Proto $scheme;
    # }

    # # API Proxy - Calculator Service
    # location /calculator/ {
    #     proxy_pass http://calculator-service.homestarter-ns.svc.cluster.local:80/calculator/;
    #     proxy_set_header Host $host;
    #     proxy_set_header X-Real-IP $remote_addr;
    #     proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    #     proxy_set_header X-Forwarded-Proto $scheme;
    # }

    # # API Proxy - Roadmap Service
    # location /roadmaps/ {
    #     proxy_pass http://roadmap-service.homestarter-ns.svc.cluster.local:80/roadmaps/;
    #     proxy_set_header Host $host;
    #     proxy_set_header X-Real-IP $remote_addr;
    #     proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    #     proxy_set_header X-Forwarded-Proto $scheme;
    # }

    # # API Proxy - Lifecycle Events (Roadmap Service)
    # location /lifecycle-events/ {
    #     proxy_pass http://roadmap-service.homestarter-ns.svc.cluster.local:80/lifecycle-events/;
    #     proxy_set_header Host $host;
    #     proxy_set_header X-Real-IP $remote_addr;
    #     proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    #     proxy_set_header X-Forwarded-Proto $scheme;
    # }

    # Static files
    location / {
        root /usr/share/nginx/html;
        index index.html index.htm;
        try_files $uri $uri/ /index.html;

        # Cache static files
        location ~* \.(jpg|jpeg|png|gif|ico|css|js)$ {
            expires 1y;
            add_header Cache-Control "public, no-transform";
        }
    }

    # Health check endpoint
    location /health {
        access_log off;
        return 200 'healthy\n';
        add_header Content-Type text/plain;
    }

    # Error pages
    error_page 500 502 503 504 /50x.html;
    location = /50x.html {
        root /usr/share/nginx/html;
    }
}
